<%= form_with(model: location, local: true) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label 'Street Address' %>
    <%= form.text_field :street_address, id: :location_street_address, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :city %>
    <!-- attribute you're saving, then where your list is, which attribute gets saved, which attribute you see -->
    <%= form.collection_select :city, Location::CITY_LIST, :to_s, :to_s, {}, {class: 'form-control'} %>
  </div>

  <div class="actions form-group">
    <%= form.submit 'Find a Bus', class: 'btn btn-default' %>
  </div>
<% end %>
